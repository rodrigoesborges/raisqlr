% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rais_pg_load.R
\name{rais_pg_load}
\alias{rais_pg_load}
\title{Carrega o cache da RAIS em PostgreSQL}
\usage{
rais_pg_load(
  catalog,
  replace = TRUE,
  dbname = Sys.getenv("dbrais", "mte_rais"),
  host = Sys.getenv("hostraispsql", "localhost"),
  user = Sys.getenv("mte_rais", "mte_rais"),
  password = Sys.getenv("pwdrais"),
  port = 5432,
  chunk.size = as.integer(1e+06),
  db = NULL
)
}
\arguments{
\item{catalog}{catalogo com \code{output_filename} (saida de \code{\link[=rais_build]{rais_build()}}).}

\item{replace}{logico (default TRUE): faz DROP + CREATE da tabela; com
FALSE exige tabela vazia/ausente e apenas acrescenta (append).}

\item{dbname, host, user, password, port}{conexao (ver Details).}

\item{chunk.size}{linhas por bloco (default 1e6).}

\item{db}{conexao DBI alternativa ja aberta (tem precedencia sobre os
argumentos de conexao).}
}
\value{
catalogo de entrada (invisivel).
}
\description{
Cria (ou substitui) as tabelas \verb{rais_vinculo_<ano>}/\verb{rais_estabelecimento_<ano>}
no PostgreSQL e as popula por blocos via COPY. A estrutura da tabela e
harmonizada entre os arquivos do mesmo ano (uniao das colunas; coluna e
texto se algum arquivo a tipa como texto, numerica caso contrario).
Cada tabela e escrita numa unica transacao (ROLLBACK automatico em erro).
}
\details{
Contrato congelado (consumido por \code{AEDi/R/upload_raispsql.R}):
tabelas \code{rais_vinculo_YYYY} e \code{rais_estabelecimento_YYYY} no schema
\code{public}, nomes de colunas legados (veja \code{\link[=rais_build]{rais_build()}}). Use
\code{\link[=rais_narrow_types]{rais_narrow_types()}} apos a carga para reduzir codigos categoricos de
\verb{double precision}/\code{integer} para \code{smallint}/\code{integer}.

Credenciais: por argumento ou, em ordem de precedencia, pelas variaveis de
ambiente compartilhadas com o AEDi: \code{dbrais} (banco, default \code{"mte_rais"}),
\code{mte_rais} (usuario, default \code{"mte_rais"}), \code{pwdrais} (senha),
\code{hostraispsql} (host, default \code{"localhost"}).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rais_db_admin.R
\name{rais_narrow_types}
\alias{rais_narrow_types}
\title{Reduz tipos das colunas categoricas das tabelas RAIS}
\usage{
rais_narrow_types(
  tables = NULL,
  dbname = Sys.getenv("dbrais", "mte_rais"),
  host = Sys.getenv("hostraispsql", "localhost"),
  user = Sys.getenv("mte_rais", "mte_rais"),
  password = Sys.getenv("pwdrais"),
  port = 5432,
  db = NULL
)
}
\arguments{
\item{tables}{vetores de nomes de tabela; default: todas as \verb{rais_*}.}

\item{dbname, host, user, password, port, db}{conexao (mesma convencao de
\code{\link[=rais_pg_load]{rais_pg_load()}}).}
}
\description{
Colunas \verb{double precision}/\code{integer} que nao se enquadram na lista de
excecoes (\verb{vl_rem*}, \code{tempo_emprego}, \code{qtd_hora_contr}) sao reescritas
como \code{smallint} (<= 32767 em valor absoluto) ou \code{integer}, apos uma unica
varredura de magnitude por tabela; a coluna \code{row.names} (artefato R do
carregamento legado) e removida. Nomes de colunas nunca mudam (contrato
AEDi). No banco 620 GB -> 325 GB (v0 -> v0.1, ago/2026).
}

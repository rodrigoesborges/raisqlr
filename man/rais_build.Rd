% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rais_build.R
\name{rais_build}
\alias{rais_build}
\title{Converte os microdados da RAIS para o dialeto contratado}
\usage{
rais_build(
  catalog,
  dir = "rais_cache",
  chunk.size = as.integer(1e+06),
  tmpdir = tempdir()
)
}
\arguments{
\item{catalog}{catalogo com coluna \code{local_file} (saida de \code{\link[=rais_download]{rais_download()}};
sem ela, \code{full_path} precisa apontar para arquivo local).}

\item{dir}{diretorio do cache de saida (default \code{"rais_cache"}).}

\item{chunk.size}{linhas por bloco (default 1e6).}

\item{tmpdir}{diretorio para temporarios (default \code{tempdir()}).}
}
\value{
catalogo acrescido de \code{output_filename} (o \code{.gz} produzido) e
\code{nlines}.
}
\description{
Extrai os \verb{.7z}, detecta o dialeto (legado: \code{sep=";"}, \code{dec=","},
ISO-8859-1; novo, desde a RAIS 2024 definitiva: \code{sep=","}, \code{dec="."},
cabecalhos \code{"X - Codigo"}, arquivos internos \verb{*.COMT}), normaliza os nomes
de colunas para o contrato congelado consumido pelo AEDi
(\code{rais_vinculo_YYYY}/\code{rais_estabelecimento_YYYY}), aplica \code{-1 -> NA} e a
correcao historica de CBO (\code{'0000-1' -> '00001'}, RAIS 2020), e grava o
cache em CSV gzip \code{sep=";"}/\code{dec="."} pronto para \code{\link[=rais_pg_load]{rais_pg_load()}}.
}

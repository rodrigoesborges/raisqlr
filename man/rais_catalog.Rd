% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rais_catalog.R
\name{rais_catalog}
\alias{rais_catalog}
\title{Cataloga os microdados da RAIS disponiveis}
\usage{
rais_catalog(
  years = NULL,
  situacao = c("qualquer", "definitiva", "parcial"),
  root_url = "ftp://ftp.mtps.gov.br/pdet/microdados/RAIS",
  dir = NULL,
  recursive = TRUE
)
}
\arguments{
\item{years}{vetor numerico de anos a reter (default: todos).}

\item{situacao}{\code{"definitiva"}, \code{"parcial"} ou \code{"qualquer"} (default).
A situacao e derivada do nome da pasta do FTP: pastas \code{"<ano> Parcial"}
sao preliminares. ATENCAO: desde a RAIS 2025 o MTE publica o release
preliminar direto na pasta \code{"<ano>"} (sem sufixo); nesses casos a
deteccao por nome nao e possivel - verifique o Comunicado na pasta.}

\item{root_url}{raiz do FTP (default oficial).}

\item{dir}{alternativamente, diretorio local contendo \verb{<ano>/*.7z}
(estrutura espelhando o FTP) ou os \verb{.7z} soltos; quando informado,
o FTP nao e consultado.}

\item{recursive}{logico; varre subpastas de \code{dir} recursivamente.}
}
\value{
\code{data.frame} com colunas \code{full_path}, \code{file}, \code{subtype}
(\code{"vinculo"}/\code{"estabelecimento"}), \code{year} (numerico), \code{situacao},
\code{size_bytes} (NA quando local), \code{folder} e \code{db_tablename}
(\verb{rais_<subtype>_<year>}).
}
\description{
Lista os arquivos de microdados (vinculos e estabelecimentos) diretamente
do FTP oficial do MTE, ou de um diretorio local com os arquivos \verb{.7z}
baixados. O catalogo alimenta \code{\link[=rais_download]{rais_download()}}, \code{\link[=rais_build]{rais_build()}} e
\code{\link[=rais_pg_load]{rais_pg_load()}}.
}
\examples{
\dontrun{
cat <- rais_catalog(years = 2024:2025)
rais_download(cat, dir = "rais_data")
cache <- rais_build(cat, dir = "rais_cache")
rais_pg_load(cache)
}
}

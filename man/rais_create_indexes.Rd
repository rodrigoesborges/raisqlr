% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rais_db_admin.R
\name{rais_create_indexes}
\alias{rais_create_indexes}
\title{Cria os indices de consulta das tabelas RAIS}
\usage{
rais_create_indexes(
  years = NULL,
  concurrently = FALSE,
  dbname = Sys.getenv("dbrais", "mte_rais"),
  host = Sys.getenv("hostraispsql", "localhost"),
  user = Sys.getenv("mte_rais", "mte_rais"),
  password = Sys.getenv("pwdrais"),
  port = 5432,
  db = NULL
)
}
\arguments{
\item{years}{anos; default: todos os \verb{rais_vinculo_<ano>} existentes.}

\item{concurrently}{usar CREATE INDEX CONCURRENTLY (default FALSE; exige
fora de transacao e so faz sentido com consumidores ativos).}

\item{dbname, host, user, password, port, db}{conexao (mesma convencao de
\code{\link[=rais_pg_load]{rais_pg_load()}}).}
}
\description{
Replica o conjunto historico de indices do banco \code{mte_rais} (criados para
as queries de indicadores do AEDi/PNDR): nos vinculos,
\verb{(vinculo_ativo_31_12, municipio)} e compostos com \code{cnae_2_0_classe},
\code{escolaridade_apos_2005}, \code{trunc(cbo_ocupacao_2002/1000)} e
\code{trunc(cbo_ocupacao_2002)}; nos estabelecimentos, \code{(municipio)} e
\verb{(municipio, cnae_2_0_classe)}.
}

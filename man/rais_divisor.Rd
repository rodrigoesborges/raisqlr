% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eras.R
\name{rais_divisor}
\alias{rais_divisor}
\title{Divisor de \code{trunc()} para um nivel de CNAE}
\usage{
rais_divisor(
  ano,
  tabela = c("vinculo", "estabelecimento"),
  nivel = c("divisao", "grupo", "classe"),
  conceito = "cnae_2_0"
)
}
\arguments{
\item{ano}{ano de referencia (a tabela \verb{_<ano>}).}

\item{tabela}{\code{"vinculo"} ou \code{"estabelecimento"}.}

\item{nivel}{\code{"divisao"} (2 digitos), \code{"grupo"} (3) ou \code{"classe"} (4).}

\item{conceito}{\code{"cnae_2_0"} (default; 7 digitos no estabelecimento a
partir de 2024) ou \code{"cnae_95"} (5 digitos em todas as eras).}
}
\value{
integer com o divisor (1000/100/10 ou 100000/10000/1000).
}
\description{
Os codigos CNAE das tabelas variam de granularidade por era (5 digitos
no vinculo em todas as eras; no estabelecimento a subclasse vem com
digito verificador - 7 digitos - a partir de 2024). Este helper devolve o
divisor que extrai \code{nivel} da coluna CNAE do ano:
\code{trunc(coluna / rais_divisor(ano, tabela, nivel))}.
}
